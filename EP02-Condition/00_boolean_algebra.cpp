#include <iostream>

using namespace std;

int main() {

    bool a = true;
    bool b = true;
    bool c = true;

    // AND &&
    cout << (a && b && c) << endl;

    // OR ||
    cout << (a || b || c) << endl;

    // NOT !
    cout << (!a) << endl;

    return 0;
}
